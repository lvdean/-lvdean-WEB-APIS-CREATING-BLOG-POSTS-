function renderBlogInput(){
    const completedBlog = JSON.parse(localStorage.getItem("allInput"));
}


renderBlogInput()
function init() {
    // When the init function is executed, the code inside renderLastGrade function will also execute
    blogInput();
  }
  init();
  